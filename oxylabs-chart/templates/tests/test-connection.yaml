apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "oxylabs-chart.fullname" . }}-test-connection"
  labels:
    {{- include "oxylabs-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "oxylabs-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
