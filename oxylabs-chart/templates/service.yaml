apiVersion: v1
kind: Service
metadata:
  name: {{ include "oxylabs-chart.fullname" . }}
  labels:
    {{- include "oxylabs-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oxylabs-chart.selectorLabels" . | nindent 4 }}
